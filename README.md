# immense

Showcase-only repository snapshot.

This version is intentionally redacted for presentation:
- `backend`, `admin-panel`, and `frontend` keep visible structure
- implementation details and runnable internals are removed
- content is not intended for direct reuse in production
